import { userConstants } from "../constants";
import { userServices } from "../services";

export const favEventsActions = {
  getEvents,
  deleteEvents
};

function getEvents() {
  /**
   * fer les trucades a la api
   */
}
